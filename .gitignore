node_modules/
.next/
out/
dist/
.env
.env.local
.env.*.local
.claude-flow/
.claude/
*.log
.DS_Store
Thumbs.db
.swarm/
.vercel/

supabase/.temp/

# typescript build info
*.tsbuildinfo
.vercel

# Python
__pycache__/
*.pyc

# Signing / push secrets — NEVER commit (Apple .p8 ASC+APNs keys, certs, profiles)
*.p8
*.p12
*.mobileprovision
*.cer
cert_key
cert_key.pub
cert_key*.pem
# Other credential material that must never be committed
*recovery*code*.txt
twilio_2FA_recovery_code.txt
# iOS VoIP push cert material (Apple VoIP Services cert + private key + CSR)
ios-voip-cert/
.env.twilio.local
